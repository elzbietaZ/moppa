/*'use strict';


 describe('Controller: AuthenticationController', function() {

    var $rootScope, $scope, $location, $controller;

    beforeEach(module('Authentication'));

    beforeEach(inject(function(_$rootScope_, _$controller_){
        $rootScope = _$rootScope_;
        $scope = $rootScope.$new();
        $controller = _$controller_;

        $controller('LoginController', {'$rootScope' : $rootScope, '$scope': $scope});
    }));

    describe('login', function(){

        beforeEach(function(){
            $scope.username = "Ela";
            $scope.password = "pass";
           // $scope.login();
        });

         it('should valid id be returned', function() {
             //   expect($rootScope.currentUser.userId).toBe();
         });

    });



});
*/